var vel = 45
console.log(`A velocidade do seu carro é ${vel}km/h`)
if (vel > 60) {  //condição simples tem apenas o if
    console.log("Voce ultrapassou a velocidade permitida, MULTADO!")
} else { //condição composta contém if e else
    console.log('Voce está na velocidade adequada para essa via, siga em frente e dirija sempre usando cinto de segurança')
}
